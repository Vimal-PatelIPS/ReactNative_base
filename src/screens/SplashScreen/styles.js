import {StyleSheet} from 'react-native';
import {ThemeUtils} from "src/utils";


export const styles = StyleSheet.create({
    logo: {
        width: ThemeUtils.relativeHeight(30),
        height: ThemeUtils.relativeHeight(30),
    }

});